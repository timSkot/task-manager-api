# Task Manager API

Простой REST API для управления задачами (todo-list), написанный на Go с нуля в учебных целях — как часть портфолио для поиска работы junior Go-разработчиком.

Проект специально построен по слоистой архитектуре (`handler → service → repository`), чтобы было легко заменить хранилище (сейчас — in-memory, дальше — PostgreSQL) без изменения бизнес-логики.

## Стек

- Go 1.22+ (стандартная библиотека `net/http`, без сторонних роутеров)
- In-memory хранилище (на данном этапе; PostgreSQL — в разработке)

## Архитектура

```
main.go              — точка входа, сборка (wiring) слоёв
internal/
  model/              — структуры данных (Task, UpdateTaskInput)
  repository/         — слой хранения данных (интерфейс + in-memory реализация)
  service/            — бизнес-логика и валидация
  handler/            — HTTP-обработчики (парсинг запросов, статус-коды)
```

Каждый слой знает только о слое ниже себя:

- **handler** знает про **service**
- **service** знает про **repository** (через интерфейс `TaskRepository`)
- **repository** ничего не знает о вышестоящих слоях

Благодаря этому `repository` можно заменить на реализацию с PostgreSQL, не трогая `service` и `handler`.

## Запуск

```bash
go run main.go
```

Сервер запустится на `http://localhost:8080`.

## API

| Метод  | Путь            | Описание                      |
|--------|-----------------|--------------------------------|
| GET    | `/tasks`        | Список всех задач              |
| POST   | `/tasks`        | Создать задачу                 |
| GET    | `/tasks/{id}`   | Получить задачу по ID          |
| PATCH  | `/tasks/{id}`   | Частично обновить задачу       |
| DELETE | `/tasks/{id}`   | Удалить задачу                 |

### Пример запроса — создание задачи

```bash
curl -X POST http://localhost:8080/tasks \
  -H "Content-Type: application/json" \
  -d '{"title":"Learn Go"}'
```

Ответ:

```json
{"id":1,"title":"Learn Go","done":false}
```

### Пример запроса — частичное обновление

```bash
curl -X PATCH http://localhost:8080/tasks/1 \
  -H "Content-Type: application/json" \
  -d '{"done":true}'
```

### Пример запроса — удаление

```bash
curl -X DELETE http://localhost:8080/tasks/1
```

Ответ: `204 No Content`

## Обработка ошибок

| Статус | Когда возвращается                                 |
|--------|-----------------------------------------------------|
| 400    | Невалидный JSON, невалидный `id`, пустой `title`     |
| 404    | Задача с указанным `id` не найдена                   |
| 500    | Непредвиденная ошибка сервера                        |

## Планы по развитию

- [ ] Подключение PostgreSQL вместо in-memory хранилища
- [ ] Миграции БД (`golang-migrate`)
- [ ] Docker + docker-compose
- [ ] Метрики (Prometheus)
- [ ] Юнит-тесты для service-слоя

## Зачем этот проект

Проект создан как учебный — с целью на практике закрепить основы Go (структуры, указатели, slice, интерфейсы, обработку ошибок) и построить работающий REST API по архитектуре, близкой к промышленной.